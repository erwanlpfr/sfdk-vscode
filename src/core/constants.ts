/** Expected stdout when the build engine or emulator is running. */
export const ENGINE_RUNNING_OUTPUT = "running: yes";

/** Suffix appended to target names in the SDK targets directory. */
export const DEFAULT_TARGET_SUFFIX = ".default";

/** Interval in ms between build engine status polls. */
export const ENGINE_POLL_INTERVAL_MS = 10_000;
